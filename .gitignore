# Originals kept locally, never deployed: the full-resolution portrait that
# assets/img/profile.jpg is cropped from, and the CV as it was before the
# declaration and signature were removed.
_source/

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
__pycache__/

# Working folders, never part of the site
design-review/
.claude/
